# statebox + react workbench

small workbench project for statebox integration with react.

to run... from the project root.

```
node tasks install
node tasks bench
```

starts a live editing server on port 5000